[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[project]
name = "archive_crate"
version = "0.1.0"
requires-python = ">=3.10"
description = "Rust performance engine for PPA (PyO3 extension)"

[tool.maturin]
features = ["pyo3/extension-module"]
