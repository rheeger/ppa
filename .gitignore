# Local credentials / host-specific paths
ppa.yml
ppa.yaml
ppa.json
.env.pgvector

# Operator logs
logs/

# Local stratified slices (slice-seed output from make slice-local-*; large vault trees)
.slices/1pct/
.slices/5pct/
.slices/10pct/

# All generated artifacts (staging, caches, benchmarks, reports)
_artifacts/

# Legacy enrichment cache at repo root (CLI defaults use _artifacts/_enrichment_cache.db)
/_enrichment_cache.db
/_enrichment_cache.db-shm
/_enrichment_cache.db-wal

# Template sampler output (regenerated via make step-11a-*)
archive_sync/extractors/specs/samples/
archive_sync/extractors/specs/samples_seed/

# Python / tooling
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# Rust (Phase 2.9 archive_crate)
archive_crate/target/
